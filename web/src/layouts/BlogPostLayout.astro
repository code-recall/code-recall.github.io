<article>
  <slot />
</article>

<style is:global>
  h1,
  h2,
  h3 {
    margin-bottom: var(--space-l);
  }

  h4,
  h5,
  h6,
  p {
    margin-bottom: var(--space-m);
  }

  .astro-code {
    margin-bottom: var(--space-m);
    padding: var(--space-m);
    border-radius: 8px;
  }
</style>
