<article class="flow">
  <slot />
</article>

<style is:global>
  nav.toc {
    margin: var(--space-m) 0;
    font-size: var(--step-0);
  }

  .toc-link {
    margin-bottom: var(--space-xs);
    color: coral;
    font-weight: bold;
  }

  .astro-code {
    margin-bottom: var(--space-m);
    padding: var(--space-m);
    border-radius: 8px;
  }
</style>
